[project]
name = "presentation-sanity"
version = "0.1.0"
description = "Build interactive HTML, PDF, and PPTX presentations from a single manifest.yaml — with variable provenance and pre-rendered manim scenes."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = [
    "pyyaml>=6.0",
]

[project.optional-dependencies]
manim = ["manim>=0.18"]
pptx = ["python-pptx>=0.6"]

[project.scripts]
presentation-sanity = "presentation_sanity.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/presentation_sanity"]
